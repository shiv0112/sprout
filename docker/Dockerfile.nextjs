FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

# Source files are mounted as volumes in docker-compose (dev mode),
# but COPY is needed for the build context to succeed.
COPY . .

EXPOSE 3000
