[project]
name = "sprout-shared"
version = "1.0.0"
description = "Shared specs, models, and helpers for the Sprout tool registry."
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
requires-python = ">=3.12"
authors = [
    { name = "Sprout", email = "hello@sprout.dev" },
]
keywords = ["ai", "agents", "tools", "registry", "sprout"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

# Core SDK shape (SproutTool / SproutToolSpec / decorators / schema). Server-only
# helpers (auth, redis cache, rate limiting) require the [server] extra.
dependencies = [
    "pydantic>=2.0",
    "pyyaml>=6.0",
    "jsonschema>=4.0",
]

[project.optional-dependencies]
# Auth + caching + rate limiting + JWT verification — only needed by Sprout
# services running as HTTP servers. Plain SDK consumers don't pull these.
server = [
    "pydantic-settings>=2.0",
    "pyjwt[crypto]>=2.8",
    "httpx>=0.27.0",
    "redis>=5.0",
    "slowapi>=0.1.9",
    "starlette>=0.40",
    "prometheus-client>=0.20",
]

[project.urls]
Homepage      = "https://github.com/aryabyte21/sprout"
Documentation = "https://github.com/aryabyte21/sprout#readme"
Repository    = "https://github.com/aryabyte21/sprout"
Issues        = "https://github.com/aryabyte21/sprout/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["sprout_shared"]

[tool.hatch.build.targets.wheel.force-include]
"sprout_shared/py.typed" = "sprout_shared/py.typed"
